#!/usr/bin/env bash
#
# This installs RubyGems from source.

FILE="rubygems-2.6.10"

cd /tmp
wget http://production.cf.rubygems.org/rubygems/${FILE}.tgz
tar xzvf ${FILE}.tgz
cd ${FILE}
sudo ruby setup.rb

# Symlink to the `gem` binary
sudo ln -s /usr/bin/gem2.6 /usr/bin/gem
